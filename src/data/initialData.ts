import { Product, Bill } from '../types';

// Fresh website initialization: Empty product inventory and bill records for client usage
export const INITIAL_PRODUCTS: Product[] = [];

export const INITIAL_BILLS: Bill[] = [];
